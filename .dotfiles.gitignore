# ignore everything by default
/*

# this is the gitignore file for this reposiory
!/.dotfiles.gitignore

# now i will explicitly list the files i want under version control
!/.gitignore
!/.gitconfig
!/.profile
!/.bash_profile
!/.bashrc

# vim config
!/.vimrc
!/.vim
/.vim/*
!/.vim/after

# a tricky pattern:
# exclude private files within a directory, but keep certain files
# inside that directory under version control
!/.ssh
/.ssh/*
!/.ssh/config
